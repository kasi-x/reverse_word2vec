# プロジェクト改善計画

## Phase 0: 現状のスナップショットコミット(1コミット)
`git add -A` で約7ヶ月分の未コミット変更(新パイプライン、scripts/14本、新srcモジュール、論文・図、削除ファイル)をすべて1コミットで保存。メッセージ例: "Snapshot: ICA pipeline refactor, eval scripts, paper (pre-cleanup)"

## Phase 1: 科学的一貫性の修正(最優先)

### 1-1. Counter-fitting の記述を実際の結果と整合させる
- 実態: 論文のheadline数値 37.9% は **CFなし** のICA空間で出ている(`scripts/eval_reranker.py` はCFを適用せずキャッシュ空間を使用)。一方リークフリー評価(`results/leakfree_comparison.json`)では CF+ICA が Hits@1 ≈ 0% に崩落。
- `paper/paper.md` と `src/generate_paper.py` の「Counter-fitting is essential」の主張を修正。CFは「リークに敏感な実験的オプション」として正直に報告(ネガティブ結果も明記)。実装前に `results/leakfree_comparison.json` と `eval_reranker.py` を再確認。
- `run_pipeline.py:7` のdocstring誤記(phase 3 = Quantitative)も修正。

### 1-2. Qualitative評価のオラクル偏を解消
- 現状: 正解語から上位軸を選び6通り試して最小ランク(`src/qualitative_eval.py:124-211`)→ Hits@1=5=10 が同一になる過大表示。
- **ブラインドモード**を新設し主指標に変更: 「クエリ語の|ICA score|が最大のラベル付き軸を反転」(正解語を一切参照しない)+ MLP+Reranker検索の結果も併記。オラクル版は「upper bound」と明示して残す。
- 再実行し `results/qualitative_eval.json` と論文テーブルを更新。

### 1-3. 再構成品質指標を有意義にする
- 100成分=100次元では誤差が常に≈0で無意味。成分数 k ∈ {10, 25, 50, 100} ごとの相対誤差を測る-tradeoff曲線に変更(`src/analysis_report.py:242-278`)。図と論文セクションを再生成。

### 1-4. generate_paper.py を完全データ駆動に
- アブストラクト等のハードコード数値(37.9/17.7/25.2/49.5/−1.48/168 など)を全廃し、結果JSONから取得。JSON欠落時は数値をでっち上げず警告してプレースホルダに。
- `leakfree_comparison.json` も論文に取り込み(限界として正直に報告)。

### 1-5. ICA空間に来歴情報を記録
- `src/ica_transformer.py` の保存処理に model / counter_fitted / vocab_limit / random_state / created_at を追加し、再現性を確保(既存キャッシュとの互換性も維持)。

## Phase 2: エンジニアリング基盤

### 2-1. README.md 新規作成
概要・手法・セットアップ(pixi)・使い方(パイプライン+scripts)・正直な結果サマリ・リポジトリ構成。

### 2-2. CLAUDE.md を実態に同期
モジュール表に antonym_classifier / reranker / counter_fitting / relevance / contrastive_retrieval を追加、`run:cf` `run:cf:ica` とscripts/を記載、CF記述を1-1に合わせ更新。

### 2-3. テスト導入(pytest)
小さな合成データで高速に回す単体テスト: ica_transformer(往復変換・保存/読込)、axis_labeler、semantic_operations(軸反転・最近傍)、counter_fitting(反発でcosineが下がる)、antonym_classifier(特徴量構築・負例サンプリングの決定性)、reranker(特徴量)、qualitative_evalブラインドモード。モデルダウンロード不要・数秒で完了する規模。

### 2-4. ruff 導入 + 既存コード整理
`pyproject.toml` に ruff/pytest 設定、pixi.toml に dev 依存追加。ruff check --fix + format を適用。

### 2-5. Taskfile.yml 拡張
`test` / `lint` / `format` タスク追加、主要evalスクリプトをタスク登録。

### 2-6. デッドコード・重複の整理
- pixi.toml から未使用の vibrato / zstandard を削除
- `src/japanese_antonym_loader.py` を削除(未使用+他マシン絶対パス hardcoded)
- scripts/ にコピーされた `unit_rows()`(4重複)・`compute_hits()`(2重複)・fold構築を `src/eval_utils.py` に抽出し各scriptから import(script間importの eval_nn_control も修正)
- `src/conceptnet_loader.py` は TSV を読む eval_discovery 等から利用するよう配線(孤独を解消)
- 削除済みモジュールを import して壊れている `notebooks/exploration.ipynb` は削除
- 小さな `results/*.json`(論文再生成に必要)はgit管理に変更(ica_space.npz は除外維持)

## Phase 3: 検証とコミット
- `task test` / `task lint` 通過確認
- キャッシュ空間で `run_pipeline.py --load-space` により qualitative → analysis → paper を再実行し、図・論文が正常生成されることを確認
- 論文の数値が全て結果JSONと一致することをスポットチェック
- 論理単位でコミット(科学修正 / エンジニアリング基盤 / クリーンアップ)

## 実行に必要な権限
pixi での dev 依存インストール、pytest/ruff 実行、パイプライン・evalスクリプト実行(python)、git コミット